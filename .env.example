# Database connection (SQLite for the prototype).
# For production, swap to a PostgreSQL URL, e.g.:
#   DATABASE_URL="postgresql://user:password@host:5432/hakeng?schema=public"
DATABASE_URL="file:./dev.db"
